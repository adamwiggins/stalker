#!/usr/bin/env ruby

usage = "stalk <jobs.rb> [<priority>]"
file = ARGV.shift or abort usage
priority = ARGV.shift || :default

require file

puts "Working #{priority} priority jobs: [ #{Stalker.jobs(priority).join(' ')} ]"

Stalker.work priority

